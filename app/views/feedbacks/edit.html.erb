<h1><%= t 'editing', :param => t('feedback', :count => 1) %></h1>

<%= render 'form' %>

<%= link_to t('show'), site_feedback_path(@site, @feedback) %> |
<%= link_to t('back'), site_feedbacks_path %>
