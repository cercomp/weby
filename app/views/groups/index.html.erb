<h1>Listing groups</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= group.name %></td>
    <td><%= link_to 'Show', site_group_path(@site, group) %></td>
    <td><%= link_to 'Edit', edit_site_group_path(@site, group) %></td>
    <td><%= link_to 'Destroy', site_group_path(@site, group), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Group', new_site_group_path %>
