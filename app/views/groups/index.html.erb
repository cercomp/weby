<fieldset>
  <legend><%= t("group.other") %></legend>

  <div class="contextual">
    <p> <%= link_to (t"new_param.masc", :param => (t"group.one")), new_site_group_path, :class => 'icon icon-add' %></p>
  </div>

  <div id="list">
    <%= render :partial => "list" %>
  </div>
</fieldset>
