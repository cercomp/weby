<fieldset>  
  <legend><%= t 'editing', :param => t('group', :count => 1) %></legend>

  <%= render 'form' %>

  <%= link_to t('show'), site_group_path(@site, @group) %> |
  <%= link_to t('back'), site_groups_path(@site) %>
</fieldset>
