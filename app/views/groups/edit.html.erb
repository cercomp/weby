<fieldset>  
  <h1><%= t 'editing', :param => t('group', :count => 1) %></h1>

  <%= render 'form' %>

  <%= link_to t('show'), site_group_path(@site.name, @group) %> |
  <%= link_to t('back'), site_groups_path %>
</fieldset>
