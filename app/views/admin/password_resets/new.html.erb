<div class='well'>
  <div class='padding-box'>
    <%= raw flash_message %>
    <div class="alert alert-info">
      <%= t(".fill_email_form") %>
    </div>
  </div>
    
  <%= form_tag admin_password_resets_path, class: 'form-horizontal' do %>
    <div class="control-group">
      <label class="string control-label">
        <abbr title="obrigatório">*</abbr> Email
      </label>
      <div class="controls">
        <%= text_field_tag "email", "#{params[:email]}" %>
      </div>
    </div>
    <div class="form-actions">
      <%= submit_tag t('submit'), class: 'btn-primary btn-large' %> 
    </div>
  <% end %>
</div>
<%= render partial: 'footer' %>
