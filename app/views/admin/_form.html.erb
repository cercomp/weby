  <% unless @repositories.nil? %>
  <%= f.radio_button_group :top_banner_id, @repositories.map{|u| { :value => u.id, :image => u.archive.url, :alt => u.description, :title => u.description }}, (will_paginate @repositories, :previous_label => t("will_paginate.previous"), :next_label => t("will_paginate.next"), :class => 'pagination ajax'), :label => t("picture"), :help => t("picture") %>
  <% end %>
