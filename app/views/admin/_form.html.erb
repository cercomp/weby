<% unless @repositories.nil? %>
  <%# FIXME Melhorar para não ficar código tão longo %>
  <%= f.radio_button_group :top_banner_id,
  @repositories.map{ |image| { :value                 => image.id,
                           :image                     => image.archive.url(:mini),
                           :alt                       => image.description,
                           :title                     => image.description }},
                           :label                     => t("picture"),
                           :help                      => t("picture") %>
                       <% end %>
