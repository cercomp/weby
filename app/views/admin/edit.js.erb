$('#form').html('<%= escape_javascript(render(:partial => 'repositories/repo',
                                              :locals => {:obj => @site,
                                                          :field => :top_banner_id,
                                                          :collection => @images })) %>');
show_selected_image('site', 'top_banner_id');

