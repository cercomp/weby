$('#form').html('<%= escape_javascript(render(:partial => 'repositories/repo',
                                              :locals => {:obj => @site,
                                                          :field => :top_banner_id,
                                                          :collection => @images,
                                                          :hint => t("help_image")  })) %>');

$('search').html(
    '<%= form_tag request.path, :method => 'get', :remote => true do %>' +
    '  <%= label_tag t("search")%>' +
    '  <%= text_field_tag :image_search%>' +
    '  <%= submit_tag t("search")%> ' +
    '<% end %>');
//$('#search').html('<%#= escape_javascript(render(:partial => 'layouts/shared/search_form' ))%>');

show_selected_image('site', 'top_banner_id');

