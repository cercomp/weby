<h1><%= t:editing, :param => t("rights") %> </h1>

<% form_for(@right) do |f| %>
  <% if @right.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@right.errors.count, "error") %> prohibited this site from being saved:</h2>
      <ul>
      <% @right.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label  t:name %><br />
  <%= f.text_field :name %><br />
  <%= f.label  t:controller %><br />
  <%= f.text_field :controller %><br />
  <%= f.label  t:action %><br />
  <%= f.text_field :action %><br />
  <p>
    <%= f.submit 'Update' %> | <%= link_to t('back'), rights_path %>
  </p>
<% end %>
