<fieldset>
<legend><%= t('editing', :param => t('component.one')) %></legend>

<%= render 'form' %>

<%= link_to t('show'), [@site,@site_component] %> |
<%= link_to t('back'), site_site_components_path %>

</fieldset>
