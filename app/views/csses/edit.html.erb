<fieldset>
  <legend><%= t 'editing', :param => t('css.one') %></legend>

  <%= render 'form' %>

  <%= link_to t('show'), site_css_path(@site, @css) %> |
  <%= link_to t('back'), site_csses_path(@site) %>
</fieldset>
