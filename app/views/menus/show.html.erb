<fieldset>
  <legend><%= t"show" %></legend>

  <p>
    <b><%= t"title" %>:</b>
    <%=h @menu.title %>
  </p>
  <p>
    <b><%= t"link" %>:</b>
    <%=h @menu.link %>
  </p>

  <p> <%= link_to t("edit"), edit_menu_path(@menu) %> | <%= link_to t("back"), menus_path %> </p>
</fieldset>
