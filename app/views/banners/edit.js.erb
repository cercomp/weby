$('#repo_list').html('<%= escape_javascript(render(partial: 'layouts/shared/search_form'),
                                            render(:partial => 'repositories/repo', :locals => {
                                                                                    :obj => @banner,
                                                                                    :field => :repository_id,
                                                                                    :collection => @images,
                                                                                    :hint => t("help_image_banner")})) %>');

$('search').html(
    '<%= form_tag request.path, :method => 'get', :remote => true do %>' +
    '  <%= label_tag t("search")%>' +
    '  <%= text_field_tag :image_search%>' +
    '  <%= submit_tag t("search")%> ' +
    '<% end %>');

