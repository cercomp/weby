<% current_site.styles.published.reorder('styles.position asc').each do |style| %>
  <!-- <%= "#{style.name} (#{style.owner.name})" %> -->
  <style type="text/css">
    <%= raw style.css %>
  </style>
<% end %>
