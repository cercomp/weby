<% if @my_sites.empty? %>
  <div class="alert">
    <%= t(".no-site-found") %>
  </div>
<% else %>
  <h3><%= t('.my-sites') %></h3>
  <ul class="thumbnails">
    <% @my_sites.each do |site| %>
      <li class="span4">
        <%= render partial: 'site', locals: {site: site} %>
        <%= link_to icon('briefcase', text: t(".dashboard"), white: true), site_admin_url(subdomain: site), class: 'btn btn-block buffer-top' if current_user %>
      </li>
    <% end %>
  </ul>
<% end %>
