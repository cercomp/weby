<fieldset>
  <legend>
    <%= t('.new_component', :param => t("components.#{params['component']}.name")) %>
  </legend>

  <%= render :partial => 'form', :locals => { :url => site_admin_components_path, :method => :post } %>
</fieldset>
