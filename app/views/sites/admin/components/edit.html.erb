<fieldset>
  <input id="component_place_holder" type="hidden" meta-data="<%= @component.place_holder %>">
  
  <legend><%= t('.edit_component', :param => t("components.#{@component.name}.name")) %></legend>

  <%= render :partial => 'form', :locals => { :url => site_admin_component_path, :method => :put } %>

</fieldset>
