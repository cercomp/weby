<fieldset> 
  <legend><%= t 'editing', :param => t('message') %> - <%= t('feedback.one') %></legend>

  <%= render 'form' %>

  <%= link_to t('show'), site_admin_feedback_path(@site, @feedback) %> |
  <%= link_to t('back'), site_admin_feedbacks_path %>
</fieldset> 
