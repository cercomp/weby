$('#repo_list').html('<%= escape_javascript(render(partial: 'repositories/repo',
                                                   locals: { obj: @page,
                                                             field: :repository_id,
                                                             collection: @images,
                                                             hint: t("help_image_page") } )) %>')

$('#event_fields').hide("fast");
console.dir("<%=j params[:type] %>")
<% if params[:type] == 'Event' %>
  $('#event_fields').
  html('<%= escape_javascript(render(:partial => 'formEvent')) %>').
  show("fade").addClass("left");
<% end %>

show_selected_image('page', 'repository_id');
