<fieldset>
  <div id="pages">
    <% unless @page.repository.nil? %>
      <%#= link_to image_tag("#{@page.repository.archive.url}", :size => "200x200", :align => "right"), @page.url, :alt => @page.repository.description, :title => @page.repository.description %>
      <%= link_to image_tag("#{@page.repository.archive.url}", :align => "right"), @page.url, :alt => @page.repository.description, :title => @page.repository.description %>
    <% end %>

    <p> <h1> <%= @page.title %> </h1> </p>
    <p> <i><%= @page.user.login %>, <%= l @page.created_at, :format => :short  %>.</i> </p>
    <h2> <%= raw @page.summary %> </h2>
    <h3> <%= raw @page.text %> </h3>
    <p> <h3> <%= @page.url %> </h3> </p>

    <%= link_to t("edit"), edit_site_page_path(@site, @page) %> | <%= link_to t("back"), :back %>
  </div>
</fieldset>
