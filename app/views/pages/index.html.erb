<fieldset>
  <legend><%= t"list_param", :param => (t"page", :count => 2)  %></legend>

  <% if params[:type] %>
    <div class="contextual">
      <p> <%= link_to (t"create_param", :param => (t"page", :count => 1)), new_site_page_path, :class => 'icon icon-add' %> </p>
    </div>
  <% end %>

  <div id="list">
    <%= render :partial => "list" %>
  </div>
</fieldset>
