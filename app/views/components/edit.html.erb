<fieldset>
  <input id="component_place_holder" type="hidden" meta-data="<%= @component.place_holder %>">
  
  <legend><%= t('editing', :param => t('component.one')) %></legend>

  <%= render :partial => 'form', :locals => { :url => site_component_path, :method => :put } %>

</fieldset>
