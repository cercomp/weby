<fieldset>
  <input id="component_place_holder" type="hidden" meta-data="<%= @site_component.place_holder %>">
  
  <legend><%= t('editing', :param => t('component.one')) %></legend>

  <%= render 'form' %>

</fieldset>
