<fieldset>  
  <legend><%= t 'editing', :param => t('component.one') %></legend>

  <%= render 'form' %>

  <%= link_to t('show'), site_component_path(@site, @component) %> |
  <%= link_to t('back'), site_component_path(@site) %>
</fieldset>
