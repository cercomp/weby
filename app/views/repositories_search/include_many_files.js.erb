$("#file_search_box").dialog("close").remove();

$("#<%= @final_files_list %>").
html("<%= escape_javascript(render "repositories_search/files_list",
                            name: @name,
                            label: @label,
                            show_selected: true,
                            many: true) %>");

set_jquery_ui();
