<fieldset> 
  <legend><%= t"editing", :param => t("archive.one") %></legend>
  
  <%= render 'form' %>
  
  <%= link_to t("show"), site_repository_path(@site, @repository.id) %> |
  <%= link_to t("back"), :back %>
</fieldset> 
