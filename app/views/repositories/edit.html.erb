<fieldset> 
  <legend><%= t"editing", :param => t("archive", :count => 1) %></legend>
  
  <%= render 'form' %>
  
  <%= link_to t("show"), site_repository_path(@site.name, @repository.id) %> |
  <%= link_to t("back"), site_repositories_path(@site.name) %>
</fieldset> 
