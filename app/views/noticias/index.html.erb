<fieldset>
  <legend><%= t"listing", :param => (t"newsletter", :count => 2 )%></legend>

  <table class='list'>
    <thead>
      <th><%= t"title" %></th>
      <th><%= t"source" %></th>
      <th><%= t"author" %></th>
      <th><%= t"date", :param => t("register")%></th>
      <th></th>
    </thead>
    <tbody>
    <% @noticias.each do |noticia| %>
      <tr class="<%= cycle 'odd', 'even' %>">
        <td><%=h noticia.title %></td>
        <td><%=h noticia.source %></td>
        <td><%=h noticia.author_id %></td>
        <td><%=h noticia.created_at %></td>
        <td>
          <% if params[:site_id] %>
            <%= link_to t("show"), :controller => 'noticias', :action => 'show', :id => noticia %>
            <%= link_to t("edit"), {:controller => 'pages', :action => 'edit', :id => noticia, :type => 'Noticia'} %> 
            <%= link_to t("destroy"), {:controller => 'pages', :action => 'destroy', :id => noticia}, :confirm => t("are_you_sure"), :method => :delete %>
          <% end %>
        </td>
      </tr>
    <% end; reset_cycle %>
    </tbody>
  </table>

  <p> <%= link_to (t"create_param", :param => (t"news", :count => 1)), :controller => 'pages', :action => 'new', :type => "Noticia" %> </p>
</fieldset>
