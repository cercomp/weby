<%= repository_search( "Selecionar fotografia", "page_image", "teacher_photo_component[image]", (Repository.find(component.image) rescue nil), multiple: false, file_types: 'image') %>

<% content_for :bottom do %>
  <div id="dialog-repository-search">
    <%= render('repositories/repository_search') %>
  </div>
<% end %>