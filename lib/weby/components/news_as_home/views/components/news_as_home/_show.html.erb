<% unless (@page = component.page).nil? %>
  <%= render :template => 'pages/show' %>
<% else %>
  <%# TODO construir uma visualização melhor para erro de página não
    # encontrada pelo componente %>
  <%= content_tag :h1, t('.page_not_found') %>
<% end %>
