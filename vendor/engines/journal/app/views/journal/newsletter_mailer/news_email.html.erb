<% image = weby_file_view(@news.image, :l, '128', nil) %>
<% if !image.nil? %>
  <% if image.include? "src=" %>
    <%= image.insert(image.index("src=")+5, @site.url) %>
  <% end %>
<% end %>
<header>
  <%= raw available_flags(@news) if @news.locales.many? %>
  <h1><%= @news.title %></h1>
</header>

<summary>
  <p>
    <%= "#{@news.user} #{l(@news.created_at, format: :short)}" if @site.view_desc_pages %>
  </p>
  <p>
    <%= raw @news.summary %>
  </p>
</summary>

<div class="body">
  <% text = @news.text %>
  <%= raw full_url_image(text, @site.url) %>
</div>
<br>
<p><a href=<%= @url %>>Não quero mais participar da lista.</a></p>
